# Claude Instructions for North

## Git Workflow

Always use a branch-per-tier (or branch-per-feature) workflow:

1. Create a branch before any commits: `git checkout -b sigilante/<description>`
2. Commit work to that branch
3. Push the branch and open a PR against master
4. Do NOT commit directly to master
